{
  "enabled": true,
  "autoLabel": "unattended",
  "cwd": ".",
  "maxConcurrent": 1,
  "gates": "npm test",
  "defaults": {
    "timeoutMin": 30,
    "model": "default",
    "notify": "failure",
    "permMode": "auto"
  }
}
